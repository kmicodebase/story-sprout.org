---
/**
 * A truthful placeholder for sections with no verified content yet. Never
 * dress this up as if content is coming on a known date.
 */
interface Props {
  title: string
}
const { title } = Astro.props
---

<div class="empty">
  <h2>{title}</h2>
  <slot />
</div>
